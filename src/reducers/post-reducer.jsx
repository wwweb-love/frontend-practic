const initialPostState = {

}

export const postReducer = (state = initialPostState, action) => {
    switch (action.type) {
        // case 

        default: {
            return state
        }
    }
}